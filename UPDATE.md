# 自動更新セッションへの指示書

このファイルは、毎朝スケジュール実行される Claude セッションが読む「作業手順書」です。
（このファイルを直接編集すれば、更新のやり方を変えられます。）

## 目的

`data/news.js` を最新の経済ニュースで書き換え、コミット & push すること。
push されると GitHub Actions が GitHub Pages を自動再デプロイします。

## 手順

1. **ブランチを確認**
   - 作業ブランチは `claude/japan-world-economics-news-kjasrf`。
   - 最新を取得: `git fetch origin` → 対象ブランチを checkout。

2. **最新ニュースを収集**（WebSearch を使用）
   以下の3カテゴリについて、当日〜直近数日の重要ニュースを **各3件程度** 集める:
   - **マクロ経済・金融政策** (`macro`): 日銀・FRB・ECBの金利/政策、GDP、物価、雇用統計 など
   - **市場・株・為替** (`markets`): 日経平均、NYダウ、ドル円、原油、金利 など前日の相場
   - **国際・地政学** (`geopolitics`): 貿易・関税、資源、紛争、地政学リスク など

   検索クエリ例（日付は当日に置き換える）:
   - `日銀 FRB 金融政策 金利 最新`
   - `日経平均 ダウ ドル円 相場 [前営業日の日付]`
   - `世界経済 地政学リスク 貿易 関税 最新`

3. **相場サマリー（ticker）を更新**
   `market_snapshot` に日経平均・NYダウ・ドル円・米長期金利・原油などの直近値と前日比を反映。
   - `dir` は `"up"`（上昇）/ `"down"`（下落）/ `"flat"`。
   - 注: 色は日本式（上昇=赤、下落=緑）で表示される。

4. **`data/news.js` を書き換え**
   - スキーマは既存ファイルと同じ（`window.NEWS_DATA = {...}`）を厳守。
   - `updated_at` を実行時刻（JST, ISO8601）に更新。
   - 各ニュースには `headline / summary / region / source / url / date` を必ず入れる。
   - `summary` は事実ベースで2〜4文。憶測や煽りは避ける。
   - `url` は実在する出典ページを入れる（存在しないURLを作らない）。

5. **コミット & push**
   ```
   git add data/news.js
   git commit -m "chore: [YYYY-MM-DD] 経済ニュースを更新"
   git push -u origin claude/japan-world-economics-news-kjasrf
   ```
   （ネットワークエラー時は 2s/4s/8s/16s のバックオフで最大4回リトライ）

6. **PRは作らない**（明示的な依頼がない限り）。データ更新のpushのみでよい。

## 注意

- ソースが確認できない情報は載せない。数値は出典と一致させる。
- サイト本体（`index.html` / `assets/`）は原則いじらない。壊れていたら報告する。
- 収集できたニュースが少ない場合でも、取れた分だけ正確に反映する。
